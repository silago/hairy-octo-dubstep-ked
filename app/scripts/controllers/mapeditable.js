'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:MapeditableCtrl
 * @description
 * # MapeditableCtrl
 * Controller of the frontendApp
 */
angular.module('frontendApp')
  .controller('MapeditableCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
